# Use mold for faster incremental development builds on native Linux.
# This target-specific configuration leaves macOS and Windows toolchains unchanged.
[target.x86_64-unknown-linux-gnu]
linker = "clang"
rustflags = ["-C", "link-arg=-fuse-ld=mold"]
