# Cagent documentation

The user documentation is an Astro Starlight site. Pages live in `src/content/docs/`, and the
sidebar is configured in `astro.config.mjs`.

```sh
bun install
bun run dev
bun run build
```

Run commands from this directory. The production site is written to `dist/`.
